import { MainMenuClickOutsideDirective } from './main-menu-click-outside.directive';

describe('MainMenuClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new MainMenuClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
