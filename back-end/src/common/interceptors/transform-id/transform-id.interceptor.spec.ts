import { TransformIdInterceptor } from './transform-id.interceptor';

describe('TransformIdInterceptor', () => {
  it('should be defined', () => {
    expect(new TransformIdInterceptor()).toBeDefined();
  });
});
