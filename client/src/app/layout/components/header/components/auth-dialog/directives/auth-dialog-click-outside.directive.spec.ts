import { AuthDialogClickOutsideDirective } from './auth-dialog-click-outside.directive';

describe('DialogClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthDialogClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
