import { UpdateUserInSessionInterceptor } from './update-user-in-session.interceptor';

describe('UpdateUserInSessionInterceptor', () => {
  it('should be defined', () => {
    expect(new UpdateUserInSessionInterceptor()).toBeDefined();
  });
});
